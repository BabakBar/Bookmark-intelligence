
[project]
name = "bookmark-intelligence"
version = "0.1.0"
description = "Clean and format bookmarks for AI and database use"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4>=4.12.0",
    "html5lib>=1.1",
]
